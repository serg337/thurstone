// A strict success or terminal-failure package is populated only after the one production WebMCP
// sequence terminates. Null means no measured sequence has started or terminated; it is the only
// state that may render as pending.
export const MEASURED_INVOCATION_INTEGRITY_EVIDENCE: unknown = null;
